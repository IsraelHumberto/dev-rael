/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["flowbite.s3.amazonaws.com", "images.pexels.com"], // Add any other domains as needed
  },
};

module.exports = nextConfig;
